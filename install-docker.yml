---
- name: Install Docker
  hosts: all
  become: true
  tasks:
    - name: Update apt package index
      yum:
        update_cache: yes

    - name: Install Docker
      yum:
        name: docker.io
        state: present

    - name: Start and enable Docker service
      service:
        name: docker
        state: started
        enabled: yes
